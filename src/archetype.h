#pragma once
#include "types.h"
#include <cstddef>
#include <expected>
#ifndef ARCHETYPE_H
#define ARCHETYPE_H

class ArchetypeManager {
public:
//ArchetypeManager();
//~ArchetypeManager();
//
//std::expected<void, EcsError> add_entity(entity_id e_id);
//std::expected<void, EcsError> remove_entity(entity_id e_id);
//
//std::expected<void, EcsError> add_component_entity(entity_id e_id, component_id c_id, size_t component_size, char* component_data);
//std::expected<void, EcsError> remove_component_entity(entity_id e_id, component_id c_id);

};

#endif